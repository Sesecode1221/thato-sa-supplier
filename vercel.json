{
  "version": 2,
  "builds": [
    {
      "src": "api/graphql.js",
      "use": "@vercel/node"
    },
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/graphql.js" },
    { "src": "/(.*)", "dest": "/frontend/$1" }
  ]
}
